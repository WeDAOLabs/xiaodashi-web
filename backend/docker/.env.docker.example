# =============================================================================
# Docker 容器编排配置模板 - xiaodashi-web Backend
# =============================================================================
# 使用说明：
# 1. 此文件仅管理 Docker 容器编排相关配置（镜像、资源、日志等）
# 2. 应用配置（数据库、JWT、Redis 等）请在同目录的 .env 文件中管理
# 3. 复制 .env.example 为 .env 并填入应用配置的实际值
# 4. 复制本文件为 .env.docker 并填入 Docker 配置的实际值
# 5. 部署时需要两个文件：.env (应用配置) + .env.docker (Docker 配置)
# =============================================================================

# ============================================================================
# Docker Compose 项目配置
# ============================================================================

# Docker Compose 项目名称（用于容器组标识和管理）
COMPOSE_PROJECT_NAME=xiaodashi-app-backend

# ============================================================================
# 镜像配置
# ============================================================================

# 镜像完整名称（可包含仓库地址和命名空间）
# 示例：
#   - 本地镜像: xiaodashi-backend
#   - 阿里云: registry.cn-hangzhou.aliyuncs.com/your-namespace/xiaodashi-backend
#   - Harbor: harbor.example.com/production/xiaodashi-backend
IMAGE_NAME=registry.cn-beijing.aliyuncs.com/huyuan/xiao-da-shi-app-backend

# 镜像版本标签
IMAGE_TAG=latest

# ============================================================================
# 容器资源限制
# ============================================================================

# CPU 限制（核心数）
CPU_LIMIT=2.0

# 内存限制
MEMORY_LIMIT=2G

# CPU 预留（核心数）
CPU_RESERVATION=0.5

# 内存预留
MEMORY_RESERVATION=512M

# ============================================================================
# Docker 日志配置
# ============================================================================

# 单个日志文件最大大小
LOG_MAX_SIZE=10m

# 保留的日志文件数量
LOG_MAX_FILES=3

# ============================================================================
# 阿里云镜像仓库凭证（可选）
# ============================================================================

# 阿里云访问密钥 ID（用于推送镜像）
ALIYUN_DOCKER_USERNAME=your-aliyun-username

# 阿里云访问密钥 Secret
ALIYUN_DOCKER_PASSWORD=your-aliyun-secret-key

# ============================================================================
# 配置检查清单
# ============================================================================
#
# 部署前请确认：
# ✅ 已设置正确的镜像名称和标签
# ✅ 已配置合适的资源限制
# ✅ 已准备好 .env 文件（应用配置）
# ✅ 生产环境已设置阿里云访问凭证（如需推送镜像）
#
# ============================================================================
